<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.active4j.hr.activiti.biz.dao.FlowGetSpeRole">
    <select id="getCarAdminrole" resultType="string">
        SELECT ROLE_CODE FROM sys_role where ROLE_NAME='车辆管理员'
    </select>

    <select id="getUserRoleName" resultType="string">
        SELECT
	        ROLE_NAME
        FROM
	        sys_role b
	    LEFT JOIN sys_user_role a on a.role_id=b.id
        WHERE
	        a.USER_ID = #{userId}
    </select>

    <select id="getGoodsAdminrole" resultType="string">
        SELECT ROLE_CODE FROM sys_role where ROLE_NAME='物品管理员'
    </select>
    <select id="getSealAdminrole" resultType="string">
        SELECT ROLE_CODE FROM sys_role where ROLE_NAME='公章管理员'
    </select>

</mapper>